{
	"name": "Codespace 1", 

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [80],

	"postCreateCommand": "sudo apt update && sudo apt install apache2 php -y && sudo service apache start && sudo echo 'done!' > /workspaces/github/message.txt"
}
